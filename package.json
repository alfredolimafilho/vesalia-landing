{
  "name": "vesalia-landing",
  "version": "1.0.0",
  "description": "Vesalia — Clinical Operations · Landing page",
  "private": true,
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard --no-port-switching",
    "dev": "serve . -l 3000 --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
